name: cimmich-stock-lifecycle

services:
  immich-server:
    image: ghcr.io/immich-app/immich-server:v3.0.3@sha256:c716dc20f957aafd89fa9d284a2ec63e25c9e2d8d8e87c6197d540a3dce237db
    volumes:
      - ${IMMICH_UPLOAD_ROOT:?set IMMICH_UPLOAD_ROOT}:/data
      - /etc/localtime:/etc/localtime:ro
    environment:
      DB_HOSTNAME: immich-database
      DB_USERNAME: immich
      DB_PASSWORD: ${IMMICH_DB_PASSWORD:?set IMMICH_DB_PASSWORD}
      DB_DATABASE_NAME: immich
      REDIS_HOSTNAME: immich-redis
      IMMICH_MACHINE_LEARNING_URL: http://immich-machine-learning:3003
    ports:
      - 127.0.0.1:${IMMICH_PORT:-22839}:2283
    depends_on:
      immich-database:
        condition: service_healthy
      immich-redis:
        condition: service_healthy
    networks:
      - immich-internal

  immich-machine-learning:
    image: ghcr.io/immich-app/immich-machine-learning:v3.0.3@sha256:d76fe88b69282c09a97eac4f82dafa82cfd77bce274bc742591cde974f87dacb
    volumes:
      - ${IMMICH_MODEL_CACHE_ROOT:?set IMMICH_MODEL_CACHE_ROOT}:/cache
    networks:
      - immich-internal

  immich-redis:
    image: docker.io/valkey/valkey:9@sha256:4963247afc4cd33c7d3b2d2816b9f7f8eeebab148d29056c2ca4d7cbc966f2d9
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping || exit 1"]
      interval: 1s
      timeout: 3s
      retries: 60
    networks:
      - immich-internal

  immich-database:
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:bcf63357191b76a916ae5eb93464d65c07511da41e3bf7a8416db519b40b1c23
    environment:
      POSTGRES_PASSWORD: ${IMMICH_DB_PASSWORD:?set IMMICH_DB_PASSWORD}
      POSTGRES_USER: immich
      POSTGRES_DB: immich
      POSTGRES_INITDB_ARGS: --data-checksums
    volumes:
      - ${IMMICH_DB_ROOT:?set IMMICH_DB_ROOT}:/var/lib/postgresql/data
    shm_size: 128mb
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U immich -d immich"]
      interval: 1s
      timeout: 3s
      retries: 60
    networks:
      - immich-internal

  cimmich-database:
    image: pgvector/pgvector:0.8.2-pg17-trixie@sha256:5c97c57367a485a8e99389548db67d441ab1a878f5492c3df04989f34ecf3c75
    environment:
      POSTGRES_PASSWORD: ${CIMMICH_DB_PASSWORD:?set CIMMICH_DB_PASSWORD}
      POSTGRES_USER: cimmich
      POSTGRES_DB: cimmich
    volumes:
      - ${CIMMICH_DB_ROOT:?set CIMMICH_DB_ROOT}:/var/lib/postgresql/data
    ports:
      - 127.0.0.1:${CIMMICH_DB_PORT:-55439}:5432
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cimmich -d cimmich"]
      interval: 1s
      timeout: 3s
      retries: 60
    networks:
      - cimmich-internal

networks:
  immich-internal:
    internal: false
  cimmich-internal:
    internal: false
